# This testdata directory is also used as a Terraform authoring module.
# Keeping a lock file lets terraform-ls recognize it as a root module and load
# the locally-built provider schema supplied by the development override.
